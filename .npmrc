auto-install-peers=true
strict-peer-dependencies=false
shared-workspace-lockfile=true
save-exact=false

# Flat node_modules so nested bundlers (Vocs) can resolve optional native bindings
# (lightningcss/esbuild/rollup) under pnpm. Standard pnpm workaround for native-dep resolution.
node-linker=hoisted
