---
"@fluxgantt/core": patch
"@fluxgantt/react": patch
"@fluxgantt/vue": patch
---

Add npm registry metadata (`repository`, `homepage`, `bugs`, `keywords`, `author`) to the
published packages, so the package page links back to the source directory and the docs site.
