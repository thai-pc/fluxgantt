---
'@fluxgantt/core': patch
---

Document the whole published API in `README.md`: undo/redo, zoom, hierarchical selection and
`duplicateTask()` shipped in 0.2.0 with no mention on the npm page. No code change — but the
README is part of the tarball, so it needs a release to reach npmjs.com.
