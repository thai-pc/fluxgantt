---
"@fluxgantt/core": patch
"@fluxgantt/react": patch
"@fluxgantt/vue": patch
---

Include `CHANGELOG.md` in the published tarball, so the release notes changesets generates are
visible to consumers on npm rather than only on GitHub.
